| 項目 | 値 |
|------|---|
| 理解度 | [x][x][x][ ] |
| 保存日 | 2026-02-25 |
| 最終復習 | 2026-03-03 |
| 次回復習 | 2026-03-10 |

**Q:** `COUNT(*) OVER()` と `MAX(total_count)` が同義になるのはなぜ？また PARTITION BY があると何が変わる？

**A:**
- `COUNT(*) OVER()`（PARTITION BYなし）は全行に**同じ値**（全体行数）を貼り付けるため、MAX しても値は変わらず同義になる
- `PARTITION BY category` を指定すると、各行に「自分が属するパーティション内の行数」が貼り付けられる（行によって値が異なる）
- そのため PARTITION BY ありの場合、`MAX` すると「最大パーティションのカウント」になり全体行数とは別物になる

**メモ:** `OVER` の基本は [[ウィンドウ関数-OVER句]] を参照。GROUP BY 文脈で値を取り出すための `MAX` の使い方は [[MAX-値取り出し-GROUP-BY制約]] を参照。WHERE と PARTITION BY の違い:
- `WHERE` → 条件に合わない行を**除外**する（行が消える）
- `PARTITION BY` → 行は残したまま、**グループ別に集計**する（行は消えない）

```sql
-- PARTITION BY category の場合
-- id | category | count
--  1 | A        | 2   ← Aグループは2行
--  2 | A        | 2
--  3 | B        | 1   ← Bグループは1行
```
