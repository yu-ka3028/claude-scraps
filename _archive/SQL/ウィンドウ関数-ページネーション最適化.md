| 項目 | 値 |
|------|---|
| 理解度 | [x][x][x][ ] |
| 保存日 | 2026-02-25 |
| 最終復習 | 2026-03-11 |
| 次回復習 | 2026-03-18 |

**Q:** ページネーションで「総件数 + 現ページデータ」を1クエリで返すとき、CTE方式とウィンドウ関数方式でどう違うか？

**A:** CTE方式は `aTable` を2回スキャンする。

```sql
-- CTE方式（非効率）
WITH total_count AS (
  SELECT COUNT(*) FROM aTable WHERE ...  -- 1回目
)
SELECT a, b, t.total_count
FROM aTable, total_count t               -- 2回目 + クロス結合
WHERE ...
```

ウィンドウ関数は1回のスキャンで完結する。

```sql
-- ウィンドウ関数方式
SELECT a, b,
  COUNT(*) OVER () AS total_count  -- LIMIT前の全件数を各行に付ける
FROM aTable t
WHERE ...
LIMIT x OFFSET y
```

**メモ:** ウィンドウ関数は「集計しながら元の行を消さない」のが特徴（[[ウィンドウ関数-OVER句]] 参照）。CTE方式の詳細は [[CTE]] を参照。`COUNT(*) OVER()` の挙動の詳細は [[COUNT-OVER-PARTITION-BY]] を参照。`GROUP BY` はグループを1行に畳むが、`OVER()` は各行を残したまま集計値を付けられる。`COUNT(*) OVER()` は `PARTITION BY` なしだと WHERE 絞り込み後・LIMIT 適用前の全件数が対象になる。
